spring:
  config:
    import: "classpath:apiKeys.yml"
  elasticsearch:
    rest:
      uris: ${instance}
      username: ${instanceUN}
      password: ${instancePW}
  data:
    elasticsearch:
      repositories:
        enabled: false