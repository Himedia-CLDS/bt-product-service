spring:
  config:
    import: "classpath:api-keys.yml"
  elasticsearch:
    rest:
      uris: ${instance}
      username: ${instanceUN}
      password: ${instancePW}
  data:
    elasticsearch:
      repositories:
        enabled: false